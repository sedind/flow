app_root: .
watcher:
  app:
    name: "Application watcher"
    watch: .
    change_command: "go"
    change_args: [build, -v, -i, -o, tmp/flow]
    post_change_command: "tmp/flow"
    post_change_args: []
    extensions:
     - .go
    ignore:
      - app
  

